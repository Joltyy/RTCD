# Copy to .env.production and set to your deployed Cloud Run service URL.
# Note the wss:// scheme and the /stream path -- both are required.
# Vite inlines this at BUILD time, so re-run `npm run build` after changing it.
VITE_BACKEND_WS_URL=wss://YOUR-SERVICE-xxxxxxxxxx-uc.a.run.app/stream
